When designing a system, keep decisions at DESIGN altitude (architecture, behavior, principles — decide now) and defer IMPLEMENTATION choices (model selection, config surface, tooling, cost numbers, exact taxonomies) to the build phase that needs them — per phase, not all up front. The user drives one topic at a time; on genuinely open design questions, offer to research rather than guess.
