A validation pass is only valid for the exact bytes it reviewed. If a file is edited after its last validator/review run — even a one-line tweak — re-run the matching validator (skill-reviewer for SKILL.md, plugin-validator for agents/manifests/commands/hooks, tests for code) before calling the task done. Before declaring done, list files changed since their last validation and re-validate each. If a shared contract file changed, re-check everything that references it.
